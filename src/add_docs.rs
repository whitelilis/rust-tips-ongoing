#[cfg(test)]
mod test {

    ///
    /// This function is just for test, wizard just add this doc.
    /// # Most time, H1 is enough
    /// ```rust,no_run
    /// fn test1() -> u8{
    ///     8;
    /// }
    /// ```

    #[test]
    fn add_doc_for_this() {
        assert_eq!(3 * 5, 15);
    }
}
